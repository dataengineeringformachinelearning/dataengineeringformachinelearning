{
  "name": "marketing",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || ^26.0.0"
  },
  "scripts": {
    "prebuild": "node -e \"if (!process.env.VERCEL) require('child_process').execSync('npm run build:viking-ui:package --prefix ..', {stdio:'inherit'})\"",
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "test": "node --test --experimental-strip-types src/lib/*.spec.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@dataengineeringformachinelearning/viking-ui": "^9.7.1",
    "astro": "^7.1.3",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "sass": "^1.101.0"
  }
}
