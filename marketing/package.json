{
  "name": "marketing",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prebuild": "npm run build:viking-ui:package --prefix ..",
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "test": "node --test --experimental-strip-types src/lib/*.spec.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@dataengineeringformachinelearning/viking-ui": "^9.4.0",
    "astro": "^7.0.3",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/marked": "^6.0.0",
    "sass": "^1.101.0"
  }
}
