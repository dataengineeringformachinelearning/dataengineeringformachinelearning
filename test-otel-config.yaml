receivers:
  otlp:
    protocols:
      http:

exporters:
  clickhouse:
    endpoint: http://127.0.0.1:8123
    database: otel

service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [clickhouse]
